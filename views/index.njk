{% extends "layout.njk" %}

{% block content %}

    <section id="introduction">
        <div id="description">
            <h1>As melhores receitas</h1>
            <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo interio.</p>
        </div>
        <img src="/assets/chef.png" alt="Imagem Cozinheiro">
    </section>

    <section id="most-viewed">
        <h1>Novas Receitas</h1>

        <div class="receitas">
            {% for item in items %}
                {% if item.id > loop.length - 6 %}
                <div class="receita">
                    <img src="{{ item.image }}" alt="Imagem de {{ item.title }}" class="burguer">
                    <h3>{{ item.title }}</h3>
                    <p>{{ item.author }}</p>
                </div>
                {% endif %}
            {% endfor %}
        </div>
    </section>  

{% endblock %}