{% extends "layout-admin.njk" %}

{% block head %}
    <title>Chefs - Foodfy</title>
{% endblock %}

{% block content %}
    <section id="most-viewed">
        <div class="header-chef">
            <h1 class="title-admin">Gerenciar Chefes</h1>
            <a href="/admin/chefs/create" class="button button-criar">Novo</a>
        </div>
        
        <div class="chefs">

            {% for chef in chefs %}
                <div class="chef">
                    <img src="{{ chef.file.src }}" alt="chef.file.name">
                    
                    <div>
                        <h3>{{ chef.name }}</h3>
                        <a href="/admin/chefs/{{ chef.id }}">Visualizar</a>
                    </div>                    
                </div>
            {% endfor %}
                 
        </div>
    </section>
{% endblock %}